<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    <title>GeoTagApp</title>
    <link rel="stylesheet"
          href="./stylesheets/style.css" />

    <!-- Leaflet CSS and JavaScript (in this order) -->
    <link rel='stylesheet' href='https://unpkg.com/leaflet@1.9.4/dist/leaflet.css'
          integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
          crossorigin=""/>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
            integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
            crossorigin=""></script>

    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
</head>

<body>
    <header class="header row">
        <div class="header-elements col-4" id="header-image-container">
            <a href="/">
                <img src="./images/location.png" id="header-image" alt="an image of a map and location">
            </a>
        </div>
        <div class="header-elements col-4" id="header-title-container">
            <h1 id="header-title">GeoTagApp</h1>
        </div>
        <div class="header-elements col-4" id="header-navigation-container">
            <nav id="header-navigation">
                <a href="#">About</a> / <a href="#">Help</a> / <a href="/">Home</a>
            </nav>
        </div>

    </header>

    <main class="main row">
        <section class="main__tagging tagging col-3">
            <h2>Tagging</h2>

            <!-- ############### Tag Form-->
            <form id="tag-form"
                  action="/tagging"
                  method="post">

                <fieldset id="tag-form-fieldset">
                    <legend id="tag-form-legend">Add a new GeoTag</legend>

                    <label id="tag-latitude-label" for="tag-latitude-input">Latitude</label>
                    <input type="text"
                           id="tag-latitude-input"
                           name="tagLatitude"
                           value="<%= locals.tagLat %>"
                           readonly>
                 
                    <label id="tag-longitude-label" for="tag-longitude-input">Longitude</label>
                    <input type="text"
                           id="tag-longitude-input"
                           name="tagLongitude"
                           value="<%= locals.tagLong %>"
                           readonly>

                    <label id="tag-name-label" for="tag-name-input">Name</label>
                    <input type="text"
                           id="tag-name-input"
                           name="tagName"
                           placeholder="e.g. Koeri-Werk"
                           maxlength="10"
                           required>

                    <label id="tag-hashtag-label" for="tag-hashtag-input">Hashtag</label>
                    <input type="text"
                           id="tag-hashtag-input"
                           name="tagHashtag"
                           placeholder="e.g. #essen"
                           pattern="#[A-Za-z]+"
                           maxlength="11">

                    <input type="submit"
                           id="tag-form-submit"
                           value="Create Tag">

                </fieldset>

            </form>
        </section>

        <section class="main__discovery discovery col-9">
            <h2 class="discovery__title">Discovery</h2>

            <!-- ############### Discovery Form -->
            <form class="discovery__search" id="discovery-form" action="/discovery" method="post">

                <fieldset id="discovery-fieldset">
                    <legend id="discovery-legend">Search</legend>

                    <label id="discovery-search-label" for="discovery-search-input">Search Term</label>
                    <input type="text" 
                    id="discovery-search-input"
                    name="discoverySearch" 
                    placeholder="e.g. Hochschule Karlsruhe">

                    <input type="hidden" 
                    id="discovery-latitude-input"
                    name="discoveryLatitude"
                    value="<%= locals.disLat %>">

                    <input type="hidden" 
                    id="discovery-longitude-input"
                    name="discoveryLongitude"
                    value="<%= locals.disLong %>">

                    <input type="submit" 
                    id="discovery-search-submit" 
                    value="Search">

                </fieldset>
            </form>

            <!-- ############### Discovery Wrapper -->
            <div class="discovery__wrapper row">
                <div class="discovery__taglist col-4">
                    <fieldset id="discovery-results-fieldset">
                        <legend id="discovery-results-legend">Results</legend>
                        <ul class="discovery__results"
                            id="discoveryResults">
                        <% if (taglist && taglist.length > 0) { %>
                            <% for(let i = 0; i < taglist.length; i++) { %>
                                <li>
                                    <%= taglist[i].name %> ( <%= taglist[i].latitude %>, <%= taglist[i].longitude %> )
                                    <%= taglist[i].hashtag %>
                                </li>
                            <% } %>
                        <% } %>
                        </ul>

                <div class="discovery_nav">
                    <div class="discovery_nav_item">
                        <button type="button" id="discovery-nav-left"><</button> 
                        <button type="button" id="discovery-nav-right">></button> 
                    </div>
                    <p class="discovery_nav_item" id="page-number">1</p>
                </div>

                    </fieldset>
                </div>

                <div id="map" class="discovery__map col-8"
                    data-tags="<%= JSON.stringify(taglist) %>">
                </div>
        </section>

    </main>

    <footer class="footer">
        <span>(c) 2021/22 HKA_IWI_VSYS</span>
    </footer>

    <!-- Load JavaScripts -->
    <script src="./javascripts/location-helper.js"></script>
    <script src="./javascripts/map-manager.js"></script>
    <script src="./javascripts/geotagging.js"></script>
</body>

</html>